{
  "name": "@juris-agi/sdk",
  "version": "1.0.0",
  "description": "TypeScript SDK for JURIS-AGI services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "npx openapi-typescript-codegen --input ../contracts/openapi.evidence.yaml --output ./src/evidence --client fetch && npx openapi-typescript-codegen --input ../contracts/openapi.juris-agi.yaml --output ./src/juris-agi --client fetch",
    "build": "tsc",
    "prepublish": "npm run generate && npm run build"
  },
  "dependencies": {
    "openapi-fetch": "^0.9.0"
  },
  "devDependencies": {
    "openapi-typescript-codegen": "^0.25.0",
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "@juris-agi/contracts": "*"
  }
}
